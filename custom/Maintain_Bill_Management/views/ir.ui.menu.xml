<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record model="ir.actions.act_window" id="action_list_bill_management">
        <field name="name">Bill management</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">bm.bill</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="bm_bill_tree"/>
        <field name="context">{'form_view_initial_mode': 'edit', 'force_detailed_view': 'true'}</field>
        <field name="search_view_id" ref="bm_bill_search"/>
    </record>

    <menuitem id="menu_bill_management" parent="" name="Bill management test" sequence="10"
        action="action_list_bill_management" groups="base.group_user"/>

    <!-- Office -->
    <record model="ir.actions.act_window" id="action_bm_office">
        <field name="name">Bill office</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">company.office.custom</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="bm_office_tree"/>
        <field name="context">{'form_view_initial_mode': 'edit', 'force_detailed_view': 'true'}</field>
    </record>

    <menuitem id="menu_bm_invoice" parent="" name="Bill management" sequence="10"
        action="action_bm_office" groups="base.group_user"/>
</odoo>